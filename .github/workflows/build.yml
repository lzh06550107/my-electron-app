#https://github.com/electron/forge/issues/2821
name: Build and Release

on:
  push:
    branches:
      - main

jobs:
  release:
    name: build and release electron app
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ windows-latest, macos-latest, ubuntu-latest ]
        arch: [x64, ia32]
        include:
          - os: macos-latest
            arch: arm64

    env:
      GITHUB_TOKEN: ${{ secrets.ELECTRON_TOKEN }}

    steps:
      - name: Github checkout
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Publish Electron App
        if: matrix.os == 'windows-latest'
        run: npm run publish -- --arch=${{ matrix.arch }} --platform=win32
      - name: Publish Electron App
        if: matrix.os == 'macos-latest'
        run: npm run publish -- --arch=${{ matrix.arch }} --platform=darwin
      - name: Publish Electron App
        if: matrix.os == 'ubuntu-latest'
        run: npm run publish -- --arch=${{ matrix.arch }} --platform=linux
